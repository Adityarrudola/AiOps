apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "aiops.fullname" . }}-sm
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: {{ include "aiops.name" . }}
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
