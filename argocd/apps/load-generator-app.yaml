apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: load-generator-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/Adityarrudola/AiOps.git'
    targetRevision: HEAD
    path: charts/load-generator
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
